load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "bmi",
    srcs = [
        "bmi_interface.c",
        "bmi_port.c",
    ],
    hdrs = glob(["*.h", "BMI/*.h"]),
    includes = ["."],
    visibility = ["//visibility:public"],
    linkopts = ["-lpcap"],
)
